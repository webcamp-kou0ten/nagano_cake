<table class="table border">
  <thead>
    <tr>
      <th class="text-center">ジャンル検索</th>
    </tr>
  </thead>
  <tbody>
    <% genres.each do |genre| %>
    <tr>
      <td><%= link_to genre.name, search_path('value': genre.id,) %></td>
    </tr>
    <% end %>
  </tbody>
</table>