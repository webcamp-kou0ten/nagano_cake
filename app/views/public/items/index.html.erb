<div class="container mt-5 mb-5">
  <div class="row">
    <div class="col-3">
      <%= render 'homes/jenre' %>
    </div>
    <div class="col-9">
      <div class="title-text row align-bottom">
        <h2>商品一覧</h2><h4>(全×件)</h4>
        <p class="text-danger">
          ※ジャンル検索の場合、「商品一覧」の部分は「（ジャンル名）一覧」に変更する<br/><例>「ケーキ一覧」
        </p>
      </div>
      <div class="row overflow-wrap mt-3 mb-5">
        <% @items.each do |item| %>
          <div class="col-3 mt-4">
            <%= attachment_image_tag(item, :image_id, :fill, 50, 50, fallback: "") %>
            <section class="image-text">
              <p class="titel"><%= item.name %></p>
              <p><strong>¥<%= itme.price %></strong></p>
            </section>
          </div>
        <% end %>
        <%= paginate @item %>
      </div>
    </div>
  </div>
</div>