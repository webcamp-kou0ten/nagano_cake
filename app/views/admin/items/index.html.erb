<div class="container">
  <div class="row">
    <div class="mx-auto mt-5 position-relative">
      <h2 class="p-2">商品一覧</h2>
      <div class="add_item">
        <%= link_to "商品追加", new_admin_item_path, class: "btn btn-success" %>
      </div>
      <table class="table">
        <tr>
          <th>商品ID</th>
          <th>商品名</th>
          <th>税抜価格</th>
          <th>ジャンル</th>
          <th>ステータス</th>
        </tr>
        <% @items.each do |item| %>
        <tr>
          <td><%= item.id %></td>
          <td><%= link_to item.name, admin_item_path(item) %></td>
          <td><%= item.price %></td>
          <td><%= item.genre.name %></td>
          <td>
            <% if item.sell_status %>
              <font color="gray">販売停止中</font>
            <% else %>
              <font color="green">販売中</font>
            <% end %>
          </td>
        </tr>
        <% end %>
      </table>
      <%= paginate @items %>
    </div>
  </div>
</div>